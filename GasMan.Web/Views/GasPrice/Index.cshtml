@model IEnumerable<GasMan.Web.Models.IndexViewListModel>
@using Newtonsoft.Json
@using System.Linq

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>
<section id="Chart" height="800px">
    <div class="content">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2"><canvas id="lineChart" height="400px"></canvas></div>
        </div>
    </div>
</section>



@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/chart.bundle")
<script type="text/javascript">




                var data = {
        labels: [@foreach(var str in ViewBag.Date)
                        {
                            @Html.Raw("'" + str + "'");
                            @Html.Raw(" ,");
                } ],
        datasets: [
            {
                label: "US Average Gas Prices",
                fill: false,
                backgroundColor: "rgba(75,192,192,0.4)",
                borderColor: "rgba(75,192,192,1)",
                //borderCapStyle: 'butt',
                //borderDashOffset: 0.0,
                //borderJoinStyle: 'miter',
                //pointDot: false,
                //pointHoverBackgroundColor: "rgba(75,192,192,1)",
                //pointHoverBorderColor: "rgba(220,220,220,1)",
                data: [@foreach(var str in ViewBag.UsAverage)
                        {

                            @Html.Raw(str);
                            @Html.Raw(",");
                        }],
                spanGaps: false,
            }
                        ]
    };


                        var CHART = document.getElementById("lineChart");
                        console.log(CHART);
                        var lineChart = new Chart(CHART, {
                            type: 'line',
                            data: data,
                            options: {
                                showXLabels: 11,
                                title: {
                                    display: true,
                                    text: 'US Gas Prices Over time'
                                },
                                scales: {
                                    xAxes: [{
                                        ticks: {
                                            autoSkip: true,
                                            maxTicksLimit: 11
                                        }
                                    }]
                                }
                            }
                        });



                        @*$(function chartType(type) {
                            var Dataset = new Object();
                            if (type == US)
                            {

                        @foreach(var str in ViewBag.UsAverage)
                        {

                            @Html.Raw("Dataset.push(" + str + ");");
                            @Html.Raw(",");

                        }
                                return Dataset;

                            }
                            else if (type == MID)
                            {

                                 @foreach(var str in ViewBag.MidwestAverage)
                        {

                            @Html.Raw(str);
                            @Html.Raw(",");
                        }

                            return Dataset;
                            }
                        });

                        $(function checkGas() {
                            $("input[name='Gas']").on("click", function () {
                                var gas = $(this).val();
                                if (gas == US_Average) var chart = chartType(US)
                                if (gas == Midwest_Average) var chart = chartType(MID)

                                return chart;
                            });
                            var chart = chartType(US)
                        });*@
</script>